import {} from '../actions/actions'
const initialState = {}

function ui (state = initialState, action) {
    switch(action.type) {

            //Use this for toggling the edit mode
        default:
            return initialState
    }
}

export default ui